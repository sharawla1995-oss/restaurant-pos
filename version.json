{
  "product": "Sharawla POS",
  "version": "10.5.4-beta.6",
  "channel": "beta",
  "source": "github",
  "windows": {
    "supported": "Windows 7 x64",
    "electron": "22.3.27",
    "electronBuilder": "22.10.5"
  },
  "notes": "Part 3 final backup hardening: pre-update backup is independent from persistDb()/createBackup(), writes db.export() to a unique exclusive temp file, fsyncs temp and finalized target, verifies exact non-zero size, uses atomic rename/copy fallback, and removes a created final target if any later step fails. Includes the cashier UI hotfix. Gate A/B/C and Manual Check checkOnly remain unchanged."
}
