name: Beta44 Offline Storage Check

on:
  push:
    branches:
      - beta44-offline-storage-recovery
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '16'
      - name: Run source and acceptance gates
        run: npm run check
