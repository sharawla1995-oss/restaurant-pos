'use strict';
const fs=require('fs');
const v7=fs.readFileSync('supabase-v7.sql','utf8');
const app=fs.readFileSync('app.js','utf8');
const takeover=fs.readFileSync('beta45-offline-v2-runtime-takeover.js','utf8');
const transport=fs.readFileSync('beta45-offline-v2-transport-runtime.js','utf8');
const wrapper=fs.readFileSync('beta55-delivery-settlement-shift-cash.js','utf8');
const fulfillment=fs.readFileSync('permissions-v2-owner-order-fulfillment.sql','utf8');
const assignment=fs.readFileSync('permissions-v2-owner-order-driver-assignment.sql','utf8');
const payment=fs.readFileSync('permissions-v2-owner-website-payment-review.sql','utf8');
function need(s,t,l){if(!s.includes(t))throw new Error(l+' missing: '+t)}
need(v7,'create policy orders_branch_update on public.orders','historical Orders UPDATE policy');
need(takeover,"registerOperation('order_status'","Offline takeover registry");
need(transport,"if(type==='order_status')return {rpc_name:'order_status_apply_offline_v2',rpc_payload:clone(payload)}",'Offline order-status transport');
need(transport,"if(type==='return'||type==='order_status'||type==='delivery_assign_driver')return !numericServerId(payload?.p_order_id)",'local order dependency defer');
need(wrapper,"ov2.saveOrderStatus(raw,'delivered')",'Offline delivery durable route');
need(payment,"has_action_permission_v2('orders.payment.review')",'website payment owner');
need(fulfillment,"has_action_permission_v2('orders.fulfillment.manage')",'fulfillment owner');
need(assignment,"has_action_permission_v2('orders.delivery.assign_driver')",'driver assignment owner');
const direct=(app.match(/rest\(\s*['"]orders['"][\s\S]{0,260}?method\s*:\s*['"]PATCH['"]/g)||[]);
if(direct.length!==0)throw new Error('Orders direct PATCH remains: '+direct.length);
if(/revoke\s+update\s+on\s+(table\s+)?public\.orders/i.test(fulfillment+assignment+payment))throw new Error('Premature Orders UPDATE revoke found');
console.log('PV2-F5E readiness PASS — renderer direct PATCH=0; Offline delivery/status routed; specialized owners present; closure artifact may now be prepared (SOURCE ONLY)');
