'use strict';const assert=require('assert'),fs=require('fs'),path=require('path'),root=path.resolve(__dirname,'..');const sql=fs.readFileSync(path.join(root,'point4-canonical-reconciliation-auditor.sql'),'utf8'),pkg=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8'));const has=s=>assert(sql.includes(s),`missing ${s}`);
for(const s of ['point4_canonical_reconciliation_audit_v1','balance_ledger_mismatch','snapshot_mismatch','value_mismatch','received_still_in_transit','missing_or_duplicate_stock_effect','duplicate_stock_effect','payable_math_mismatch','invoice_payable_mismatch','allocation_mismatch','allocation_scope_mismatch','settlement_mismatch','unbalanced','orphan_event_without_lines','orphan_reversal','orphan_stock_line','orphan_transfer_line','orphan_payable_line',"'runtime_reconciliation_accepted',false",'point4b2_concurrency_closed','transfer_allowed','ap_allowed','journal_allowed','evidence_digest','language sql stable security definer set search_path=\'\'','from public,anon,authenticated'])has(s);
const body=sql.slice(sql.indexOf('returns jsonb'),sql.indexOf('$$;',sql.indexOf('returns jsonb')));assert(!/\b(insert|update|delete|merge|truncate|alter|drop|create|grant|call|perform)\b/i.test(body),'auditor body must be read-only');assert(!/inventory_stock_apply_movement_v2\s*\(/i.test(body));assert(!/offline|engine_action_v2/i.test(sql));const gate='node scripts/check-point4-canonical-reconciliation-auditor.js';assert.strictEqual(pkg.scripts['check:point4-canonical-reconciliation-auditor'],gate);console.log('Point 4F-1 Canonical reconciliation auditor static check passed.');
