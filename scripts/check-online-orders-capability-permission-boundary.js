'use strict';const fs=require('fs'),app=fs.readFileSync('app.js','utf8'),eng=fs.readFileSync('restaurant-engine.js','utf8'),reg=fs.readFileSync('sharawla-navigation-registry.js','utf8');const f=[],n=(x,m)=>{if(!x)f.push(m)},can=(app.match(/function canAccessPage\([\s\S]*?\n}/)||[])[0]||'',pm=(eng.match(/const PAGE_MODULE=Object\.freeze\(\{[\s\S]*?\n  \}\);/)||[])[0]||'',fl=(eng.match(/const OPERATIONAL_FLAGS=Object\.freeze\(\{[\s\S]*?\n  \}\);/)||[])[0]||'',row=(reg.match(/\{routeKey:'onlineOrders'[^\n]+/)||[])[0]||'';n(/page==='onlineOrders'.*runtimeAllowsPage\(page\).*runtimeOperationalAllowsPage\(page\).*canAccessPage\('deliveryOrders'\)/.test(can),'capability+compat permission chain missing');n(!/onlineOrders:'delivery'/.test(pm)&&!/onlineOrders:'enable_delivery'/.test(fl),'Core route recoupled to Restaurant Delivery');n(/deliveryOrders:'delivery'/.test(pm)&&/deliveryOrders:'enable_delivery'/.test(fl),'Delivery gates regressed');n(/profile:'core'/.test(row)&&/module:null/.test(row)&&/pagePermission:'deliveryOrders'/.test(row),'registry boundary regressed');n(/CORE_CHANNEL_UI_EXPOSED_COMPAT_PERMISSION/.test(row),'final capability/UI marker missing');if(f.length){console.error('Online Orders Capability Permission Final Boundary: FAIL');f.forEach(x=>console.error('- '+x));process.exit(1)}console.log('Online Orders Capability Permission Final Boundary: PASS');